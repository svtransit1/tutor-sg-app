# AAAS-141: M2-18 — Kid home screen — Review Record

**Date:** 2026-05-07
**Author:** 🦋 Flutter (UX/UI Designer)
**Branch:** `feat/aaas-141-kid-home` (commit `b62884f`)
**Assignee:** 🦋 Flutter

## What passes

- **Bilingual completeness:** All 35+ UI-facing strings have EN + zh-Hans counterparts. Verified via i18n key parity check (6 top-level keys under `kidHome`, all matching between en.json and zh-Hans.json).
- **Subject tiles:** 4 tiles (Math/🧮, English/📖, Science/🔬, Chinese/🀄) in 2×2 grid with colours, icons, descriptions in both languages.
- **Hero camera button:** Prominent labelled "Snap Homework" / "拍照问功课", thumb-reachable position, navigates to `/(kid)/camera`.
- **Recent sessions:** Loads last 3 from SQLite (`getRecentSessions(3)`). Shows subject icon, subject name, time ago, question count. Empty state with prompt text.
- **Header:** Kid name (Alex, demo data) + Level 3 badge + language switcher (EN ↔ 中文).
- **Accessibility:** `accessibilityRole` and `accessibilityLabel` on header, buttons, tiles, session cards. Min 16pt body text (greeting 22pt, subject title 18pt, body 16pt). Touch targets ≥44pt (subject tiles min 160pt × 44pt, camera button min 56pt height) per ADD §9.
- **Dark mode:** All colours adapt via `useColorScheme()`.
- **SQLite storage layer:** `sessions.ts` with `insertSession`, `getRecentSessions`, `getSessionCount`. Uses `expo-sqlite` async API with CREATE TABLE IF NOT EXISTS.
- **7 unit tests** for `sessions.ts` — all passing (insert, query, count, limits, empty states).
- **No analytics SDKs** in any kid code path.
- **No data-leaving-device paths** — all data stays local SQLite.

## Known pre-existing issues (not caused by this change)

- 9 existing test failures in `ParentSignInScreen.test.tsx` and `LanguageSelectScreen.test.tsx` due to component import resolution issues. These predate this change.

## AC met

- [x] AC1: All 4 subject tiles render with correct icons and labels in both languages
- [x] AC2: Camera button navigates to camera capture screen (`/(kid)/camera`)
- [x] AC3: Recent sessions pull from SQLite (`getRecentSessions`)
- [x] AC4: VoiceOver/TalkBack labels on every interactive element
- [x] AC5: Min touch target 44pt (verified in style constants: subjectTile minHeight 160, cameraButton minHeight 56)

## Files changed

| File | Status |
|---|---|
| `mobile/app/(kid)/home.tsx` | Modified (placeholder → full implementation, 382 lines) |
| `mobile/src/i18n/locales/en.json` | Modified (added `kidHome` section, 40 lines) |
| `mobile/src/i18n/locales/zh-Hans.json` | Modified (added `kidHome` section, 40 lines) |
| `mobile/src/storage/sessions.ts` | New (SQLite session storage, 90 lines) |
| `mobile/src/storage/__tests__/sessions.test.ts` | New (7 tests, all passing) |

## Verdict

**Review: APPROVED** ✅ — Ready for 🦊 Foxy (UX + bilingual review).
