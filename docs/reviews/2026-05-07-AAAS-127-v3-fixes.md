# AAAS-127 — Owl Review Fixes (v3)

**Date:** 2026-05-07
**Owner:** Wolf (🐺)
**Branch:** `wolf/aaas-127-litert-native-module-v3`
**Commit:** `5222ad32e`

## Fixes Applied

### Fix #1 — Critical: Kotlin `exec.execute` returns `Unit`
**File:** `mobile/modules/tutor-sg-llm-runtime/src/TutorSgLlmRuntimeModule.kt`
**Change:** Removed `exec.execute { ... }` wrapper from `loadModel`, `generate`, and `unloadModel`. AsyncFunction already runs on background thread. Return `mapOf(...)` directly so JS receives actual values instead of `undefined`.

### Fix #2 — Critical: `resolveModel` defined & exported twice
**Files:** `packages/llm/src/types.ts`, `packages/llm/src/index.ts`
**Change:** Removed duplicate `resolveModel` function definition at types.ts:195-197. Removed duplicate export at index.ts:51. Also removed `import type { DeviceTier }` from `@tutor-sg/device-tier` — replaced with inline `'high' | 'mid' | 'low'` type.

### Fix #3 — High: vitest include pattern
**File:** `packages/llm/vitest.config.ts`
**Change:** Updated include pattern from `**/__tests__/**/*.vitest.ts` to `**/__tests__/**/*.test.ts`.

### Fix #4 — High: iOS `inferenceQueue.sync` blocks JS thread
**File:** `mobile/modules/tutor-sg-llm-runtime/src/TutorSgLlmRuntimeModule.swift`
**Change:** Replaced `inferenceQueue.sync { ...; return result }` pattern with `AsyncFunction(...) { (params, promise: Promise) in inferenceQueue.async { ...; promise.resolve(result) } }`. Queue still serializes access but returns immediately.

### Fix #5 — Medium: Pre-existing test failures
**Status:** Acknowledged. Pre-existing failures in `KidHomeScreen` (button role mismatch) and `ParentSignInScreen` (missing `react-native-svg` mock) are from other branches, not from this PR.

## Verification
- `packages/llm`: **6/6 tests pass** (response-parser)
- `mobile` LLM service: **19/19 tests pass** (10 LLMRuntime + 9 NativeInferenceBridge)
- Branch: `wolf/aaas-127-litert-native-module-v3`
- Commit: `5222ad32e`

## Handoff
Next reviewer: Owl (🦉) — second review pass to verify fixes.
