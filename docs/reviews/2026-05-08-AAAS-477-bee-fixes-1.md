# AAAS-477: Bee Fixes — Review Blockers Resolved

**Date:** 2026-05-08
**Branch:** feat/aaas-477-kid-accessibility-final (commit 389f02e60)
**Author:** Bee
**Reviewer:** Tortoise (previously)

## Verdict

All 4 blocking issues from Tortoise review #1 (`docs/reviews/2026-05-08-AAAS-477-tortoise-review-1.md`) have been addressed.

## Fixes Applied

### 1. Theme package complete
- Added missing files from AAAS-294 reference (index.ts, typography.ts, package.json, tsconfig.json, ThemeProvider, types, etc.)
- Created `packages/theme/src/utils/fontScale.ts` with `scaledFontSize()`, `scaledLineHeight()`, `scaledTouchTarget()` — respects `PixelRatio.getFontScale()` with 1.5x cap
- Updated `tokens/index.ts` and `src/index.ts` to export `lightColors`, `darkColors`, `kidColors`, `parentColors`, and scaled utility functions
- Added `kidColors`/`parentColors` aliases to `colors.ts` for ThemeProvider compatibility
- Added `@types/react` devDependency to theme package

### 2. Components now use theme scaling
- `ErrorBoundary.tsx`: replaced local COLORS map with `lightColors`/`darkColors` from `@tutor-sg/theme`; uses `scaledFontSize('body')` and `scaledTouchTarget()` instead of `MIN_BODY_SIZE`/`MIN_TOUCH_TARGET` constants
- `AccessiblePressable.tsx`: same treatment — theme imports, scaled size/target functions
- `_layout.tsx`: imports theme colors and scaled utilities

### 3. KidScreenHeader wired in
- Now passed as `header` option to each `Stack.Screen` (home, camera, camera-result)
- Uses `scaledTouchTarget()` for minHeight, `scaledFontSize('h3')` for title size

### 4. Dark mode title color
- Title color uses `C.textPrimary` from theme (`lightColors.textPrimary` / `darkColors.textPrimary`) instead of hardcoded `#1A1A1A`

## Verification
- Theme package typecheck: ✅ passing (`npx tsc --noEmit` in packages/theme)
- Workspace package resolution: ✅ `node -e "require.resolve('@tutor-sg/theme')"` resolves correctly
- Mobile app depends on `@tutor-sg/theme` via `mobile/package.json` workspace dependency
- `pnpm-workspace.yaml` updated to include `mobile` directory

## What's Next
- Set issue to `in_review` for Tortoise re-review
- Remaining non-blocking items from Tortoise review: ErrorBoundary `accessibilityHint` coverage

## Refs
- Issue: AAAS-477
- Previous review: `docs/reviews/2026-05-08-AAAS-477-tortoise-review-1.md`
- Standards: `docs/decisions/2026-05-07-kid-accessibility-standards.md`
- Audit: `docs/reviews/m2-accessibility-audit.md`
