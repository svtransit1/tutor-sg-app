{
  "name": "tutor-sg",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "smoke": "pnpm typecheck && pnpm lint && pnpm test",
    "format:check": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@jest/globals": "^30.0.0",
    "@types/jest": "^30.0.0",
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.8.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
