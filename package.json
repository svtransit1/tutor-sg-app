{
  "name": "tutor-sg",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.0",
    "@types/jest": "^30.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
