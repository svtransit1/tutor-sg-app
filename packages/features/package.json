{
  "name": "@tutor-sg/features",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm exec vitest --passWithNoTests",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^4.1.5"
  }
}
