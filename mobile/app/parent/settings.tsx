/**
 * Settings route — parent settings (language, quality, model, privacy).
 *
 * Per ADD §3.4: quality slider, device tier override.
 * Per ADD §6.4: analytics opt-in toggle.
 * Stub for the navigation shell.
 */
import SettingsScreen from '../../src/screens/parent/SettingsScreen';

export default function SettingsRoute() {
  return <SettingsScreen />;
}
