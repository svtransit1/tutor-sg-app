/**
 * Profiles route — child profile management.
 *
 * Stub for the navigation shell — actual implementation
 * is tracked in a separate issue.
 */
import ProfilesScreen from '../../src/screens/parent/ProfilesScreen';

export default function ProfilesRoute() {
  return <ProfilesScreen />;
}
