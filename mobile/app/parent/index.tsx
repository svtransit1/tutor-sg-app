/**
 * Parent dashboard route — session log and progress summaries.
 *
 * Per ADD §4.2: daily/weekly view, per-session details.
 * Stub for the navigation shell — actual data rendering is tracked
 * in a separate issue.
 */
import DashboardScreen from '../../src/screens/parent/DashboardScreen';

export default function DashboardRoute() {
  return <DashboardScreen />;
}
