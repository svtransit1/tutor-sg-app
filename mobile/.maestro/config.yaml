# tutor-sg Maestro E2E configuration
# Update appId when the Expo app config (app.json / app.config.ts) is finalised
appId: com.tutor-sg.app
env:
  TEST_LOCALE: en
  ONBOARDING_TIMEOUT_MS: "300000"  # 5 min for full onboarding download
